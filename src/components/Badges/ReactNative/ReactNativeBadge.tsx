import * as React from "react";

import BadgeBase from "../BadgeBase";

const ReactNativeBadge = () => {
  return (
    <BadgeBase classNames="bg-sky-100 text-sky-900">React Native</BadgeBase>
  );
};

export default ReactNativeBadge;
